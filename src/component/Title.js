import React from 'react' ;

const Title = () => (
      <div>
        <h1>City Weather finder</h1>
        <p>Find out the most recent weather conditions.</p>
      </div>
    )

export default Title;